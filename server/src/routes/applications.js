import { Router } from "express";
const router = Router();

// placeholder — implement later
router.get("/", (req, res) => {
  res.json({ msg: "applications routes placeholder" });
});

export default router;
